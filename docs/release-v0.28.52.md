# v0.28.52 — 動作を選んでアニメーションを確認

3Dビューワーでアニメーションを一つずつ選択できます。全動作の同時再生をやめ、
先頭へ戻す操作と0.25/0.5/1/2倍速を追加しました。
操作は表示専用で、制作版やGLBのキーフレーム・loop設定は変更しません。
選択を変えると先頭へ戻り、再生/一時停止状態は保持します。

## 検証と配布状態

PR #385で実装。sourceの実GLBで腕振り1秒と待機2秒を別々に再生し、
操作欄を除いた描画変化、日英・320px、停止・再表示、元ファイルのhash保持を確認しました。
非表示時停止・復帰、WebGL復旧、5回の開閉後のcontext解放も確認しています。
実装時の全1345 testsと実Three.jsミキサーの追加5 testsが成功しています。
この環境のwarm再表示は約3〜6秒で、1秒目標は未達です。
署名公開・公開4ファイル再取得・実Host署名検証を確認しました。
標準更新10.177秒でhealthyとなり、DBとBlender登録を保持しています。
本番のopaque iframeでも腕振り・待機の個別再生、速度、先頭戻し、英語/320px、
元scene保持を確認しました。ブラウザへのコード差し替えは使用していません。
最新の状態は[実装状況](implementation-status.md)に記録します。

## 互換性

公開API/schema/tool名、DB、Blender runtimeの変更はありません。
既存画像・G8加工を維持します。更新は稼働JobとGUIがない時に実施してください。
全3D Studio、ゲーム用の有機変形・IK・歩行制作・engine対応の完成を宣言する版ではありません。
