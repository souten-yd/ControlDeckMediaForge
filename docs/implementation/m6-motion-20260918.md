# M6 — additive translation clips and actual viewer playback

Source branch `ux1/3d-motion-playback`, parent PR544/0538de7. Optional
rest-bone-local translation keys preserve legacy rotation serialization and
curves. Typed GLB extras carry requested loop intent. Viewer playback honors
one-shot clips and provides explicit stop/rest restoration.

## Actual Blender and source UI

Commands and evidence:

```bash
PYTHONPATH=backend:. .venv/bin/python /tmp/mediaforge-motion-live-20260918.py
node /tmp/mediaforge-motion-browser.mjs
```

Root `/tmp/mediaforge-motion-20260918-xa_ler24`; Blender4.5.13. The prior actual
corrected limb is imported through the existing upload/hash validation path.
Sequential clips at 24fps: rotation-only idle48 (2s), translated walk24 (1s),
translated attack36 (1.5s). Times 0.431/0.433/0.441 s; final GLB 18,252 bytes.
These are synthetic motion fixtures, not natural gait or completed dinosaur clips.

Independent source inspection verifies unchanged original idle curves, head
translation extents 0.0799999233m (walk) and 0.2000000071m (attack). Actual Blender
GLB reimport evaluates 49/25/37 frames: maximum head-position errors are 0,
2.73828072369087e-09m and 8.544748560792234e-09m. Loop endpoints match. Animation
extras record true/true/false with exact FPS/frame counts. `inspection.json`
retains measured values; clip creation alone is not playback acceptance.

An isolated source MediaForge process at 127.0.0.1:19319 serves the actual Store
and current frontend, with no response replacement. Actual Chrome/WebGL2 opens
the final Asset from Library and verifies through visible controls:

- Walk remains playing past one duration and wraps time.
- Pause retains exact time; switching selects the requested clip at frame zero.
- Attack stops at 1.5 seconds and the button returns to Play.
- Stop resets time/playing; the real Three mixer test also asserts rest restoration.
- Idle can then play, and Stop remains usable. Three clips, correct durations and
  loop flags; no browser page errors. Screenshots and `browser.json` retained.

The temporary process reports setup_required for its unconfigured generation
environment. This does not represent installed image/VLM health or acceptance.
The deployed 0.28.84 service and Host were not changed. The browser run is a
source standalone viewer acceptance, not signed-installed iframe/MCP acceptance.

## Regression and remaining gates

Node's real Three mixer tests cover disjoint-track switching, explicit stop,
one-shot finish/restart and legacy repeating defaults. Python tests cover legacy
payload identity, incomplete/nonfinite/overbound translations, loop mismatch,
independent worker guards and binary-preserving extras insertion.
The first full test was stopped on its owned process tree (root1932899) to update
stale agent guidance; it is not a passed gate. The final gate is recorded below.

Finished dinosaur/quadruped idle/walk/attack, foot sliding/collision/center of mass,
real local LLM/VLM end-to-end, new signed installed projection and 27 comparison
trials remain NOT TESTED. No completed-game-asset claim follows from these fixtures.

Evidence: `/data1tb/ControlDeck/CodeDEV/MF3DS-M1-SixVertex-20260918/evidence/motion-source`.

Final full gate `./mf.sh test`: exit0, 2162 passed / 2 warnings / 228.34 s.
No product changes after the gate. The isolated viewer server was stopped.
