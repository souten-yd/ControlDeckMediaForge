# MediaForge 0.33.14

人物の自動ボーン付けが、workerで成功しても本体で`scene_recipe_worker_invalid`になる不具合を修正。
0.33.12で追加された補修報告を、対象・件数・数値上限を検証して受理する。
補修なしの空配列と旧workerの省略も受理。未知項目や不正な報告は引き続き拒否する。
公開API/ライブラリの保存形式は変更しない。既存モデルを再生成せず失敗したrig工程だけ再開できる。
