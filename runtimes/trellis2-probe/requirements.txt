# TRELLIS.2 / Pixal3D 採用 probe 専用。core とも image runtime とも混ぜない。
#
# 混ぜられない理由は版が食い違うためである。上流 Pixal3D は transformers 4.57.3 /
# diffusers 0.37.1 を要求するが、runtimes/rocm-torch は transformers 5.15.1 /
# diffusers 0.40.0 で動いている。同じ venv へ入れるとどちらかが壊れる。
#
# torch は AMD 公式 ROCm 7.2.1 wheel を固定する。上流 setup.sh の HIP 分岐は
# torch 2.6.0 + rocm6.2.4 を指すが、この機体は ROCm 7.2.1 / gfx1201 なので従わない。
# **版を固定すること。** 固定せずに入れると pip が CUDA 版 torch を引いて
# ROCm の torch を置き換える（rocm-torch/requirements.txt に実機事故の記録がある）。
--find-links https://repo.radeon.com/rocm/manylinux/rocm-rel-7.2.1/
numpy>=2.0,<3
torch==2.10.0
torchvision==0.25.0

# 上流 setup.sh --basic 相当。gradio と tensorboard は probe に要らないので入れない。
imageio==2.37.2
imageio-ffmpeg==0.6.0
tqdm==4.67.1
easydict==1.13
opencv-python-headless==4.12.0.88
ninja==1.13.0
trimesh==4.10.1
transformers==4.57.3
zstandard==0.25.0
kornia==0.8.2
timm==1.0.22
pandas==2.3.3
lpips==0.1.4
pillow==12.0.0
plyfile==1.1.3

# 上流は git 直参照だが、probe の再現性のため revision を固定する。
utils3d @ git+https://github.com/EasternJournalist/utils3d.git@9a4eb15e4021b67b12c460c7057d642626897ec8

# ネイティブ拡張（o-voxel / CuMesh / FlexGEMM / nvdiffrast / nvdiffrec）は
# ここに書かない。HIP でのビルド可否そのものが probe の第一関門なので、
# 段ごとに手で入れて結果を記録する。docs/implementation/g9-image-to-3d.md §2a。
#
# flash-attn は入れない。上流が ATTN_BACKEND=sdpa で代替できると明記しており、
# HIP 分岐も GPU_ARCHS=gfx942（MI300）決め打ちで gfx1201 には合わない。
