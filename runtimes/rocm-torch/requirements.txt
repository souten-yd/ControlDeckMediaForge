# AMD official ROCm 7.2.1 wheel repository.
# Keep this runtime separate from the Media Forge core environment.
--find-links https://repo.radeon.com/rocm/manylinux/rocm-rel-7.2.1/
numpy>=2.0,<3
torch==2.10.0
diffusers==0.40.0
transformers==5.15.1
accelerate==1.14.0
safetensors==0.8.0
huggingface-hub==1.28.0
sentencepiece==0.2.2
# LoRA の適用に要る。diffusers の load_lora_weights は PEFT backend を通す。
peft==0.19.0
# 拡大に要る。spandrel は重みから網の形を読むので、系統ごとに実装を書かずに
# 済む。torchvision は 2 つの architecture でしか使われないが、宣言された依存
# なので入れないと import できない。
#
# **版を固定すること。** 固定せずに `pip install torchvision` を打つと、pip が
# PyPI の最新を選び、それが要求する CUDA 版 torch で ROCm の torch を置き換える
# （実機で 2026-09-01 に起こした。torch 2.13.0+cu130 になり `cuda.is_available()`
# が False、transformers の遅延 import まで壊れた）。固定すれば find-links の
# ROCm wheel が選ばれ、torch は触られない。
torchvision==0.25.0
einops==0.8.2
spandrel==0.4.2

# 被写体の形を推定する経路（worker_packs/image/matte.py）。CPU 版でよい——
# GPU は画像モデルが使っており、取り合いにすると生成のほうが遅くなる。実測で
# 512² も 1024² も 4 秒である。ROCm 版の onnxruntime を入れると、上の torch と
# 同じ轍（別の加速器版が依存を置き換える）を踏むので入れない。
onnxruntime==1.29.0

# text_encoder を int8 にする。FLUX.2 は重みの 8 割が text_encoder（Qwen3）で、
# ここを削ると生成の山が 3 GB 下がる（実測 18.35 → 15.30 GiB、生成も 4.0 → 2.8 秒）。
# 同じ seed・同じ手数で絵の違いは見て取れなかった。
#
# **--no-deps で入れること。** 素で入れると pip が torch を解決し直し、
# CUDA 版で ROCm の torch を置き換える（上の torchvision と同じ事故になる）。
# int4 は quanto が HIP の拡張をその場で組もうとして失敗する。int8 は要らない。
optimum-quanto==0.2.7
